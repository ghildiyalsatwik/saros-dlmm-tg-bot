export const startHandler = (userId) => {

    console.log(`Start handler called by userId: ${userId}`);

    const reply = 'Welcome to the Saros DLMM bot. Please tell me what to do.';

    return reply;
};